{
  "assert": "stream_json_tool_use",
  "expect_tool_use": {
    "name_regex": "^(Edit|Write)$",
    "path_regex": "docs/helix/01-frame/prd\\.md$"
  },
  "secondary": {
    "assert": "prose_match",
    "must_not_match": [
      "(?i)helix-library.*(not installed|missing)",
      "(?i)setup gap"
    ]
  },
  "rationale": "Library present + helix routes; edit the canonical PRD path with no setup-gap."
}
