{
  "assert": "stream_json_tool_use",
  "expect_tool_use": {
    "name_regex": "^Write$",
    "path_regex": "^docs/helix/02-design/.*\\.md$",
    "exactly_one": true
  },
  "forbid_tool_use": [
    {
      "name_regex": "^Write$",
      "path_regex": "^docs/adr/"
    },
    {
      "name_regex": "^Write$",
      "path_regex": "^(?!docs/helix/02-design/).+\\.md$"
    }
  ],
  "secondary": {
    "assert": "prose_match",
    "must_not_match": [
      "(?i)helix-library.*(not installed|missing)"
    ],
    "allow_match_any": [
      "(?i)treating this as helix",
      "(?i)use helix-infra to override",
      "(?i)disambiguat"
    ]
  },
  "rationale": "Product-shaped repo (docs/helix/, no *.tf) → helix wins. EXACTLY ONE Write to docs/helix/02-design/*.md. The second forbid_tool_use is a universe-bounding rule: any markdown Write outside docs/helix/02-design/ (third-location escapes like 'decisions/' or 'adr/0001.md') is forbidden, not just docs/adr/."
}
