id: data-design
name: Data Design
version: 1.0.0
summary: |
  Defines bronze/silver/gold schemas, storage layouts, and dataset-level mechanics.

required_sections:
  - bronze_schema

section_aliases:
  bronze_schema:
    - "Bronze Schema"
    - "Bronze Schema (parquet)"

tags: [data, helix-data, design]
