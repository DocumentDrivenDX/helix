# Archive

Working documents that were once under `docs/helix/` but are not instances of
a catalog artifact type: dated plans, bench and phase result reports,
restoration-decision memos, research notes, and two operational documents
for the DDx runtime (`ddx-server` runbook and monitoring setup) that belong
with DDx, not HELIX. They are kept for history and are not published to the
site or treated as governing artifacts. Paths under `helix/` mirror where
each file used to live.
